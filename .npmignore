.github/
.gitignore
tests/
scripts/
node_modules/
.DS_Store
*.tgz
release.config.cjs
sentinel.config.ts
graph-out/

# Only needed at the raw GitHub URL referenced by `pi.image` in package.json;
# shipping it would add ~1.2 MB to every install for nothing.
screenshot.png
